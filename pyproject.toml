[project]
name = "meal-planning-web-app"
version = "0.1.0"
description = "Meal planning web app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "envbind",
    "pydantic",
    "pymongo>=4.16.0",
]
